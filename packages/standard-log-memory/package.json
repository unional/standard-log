{
  "name": "standard-log-memory",
  "version": "1.0.1",
  "description": "In memory appender for `standard-log`",
  "homepage": "https://github.com/unional/logging",
  "bugs": {
    "url": "https://github.com/unional/logging/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/unional/logging.git"
  },
  "license": "MIT",
  "author": {
    "name": "Homa Wong",
    "email": "homawong@gmail.com"
  },
  "files": [
    "dist",
    "lib"
  ],
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json && tsc -p tsconfig.libm.json",
    "bundle": "webpack",
    "dc": "yarn dependency-check",
    "dependency-check": "dependency-check . --unused --no-dev && dependency-check . --missing --no-dev",
    "test": "jest",
    "verify": "rimraf dist lib libm && yarn test -- --reporters=default && yarn build && yarn dc && yarn bundle",
    "preversion": "yarn verify",
    "watch": "jest --watch"
  },
  "dependencies": {
    "standard-log-core": "^1.1.1",
    "upper-case": "^1.1.3"
  }
}
