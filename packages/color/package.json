{
  "name": "aurelia-logging-color",
  "version": "1.0.0",
  "description": "ColorAppender for `aurelia-logging`",
  "homepage": "https://github.com/unional/logging",
  "bugs": {
    "url": "https://github.com/unional/logging/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/unional/logging.git"
  },
  "license": "MIT",
  "author": {
    "name": "Homa Wong",
    "email": "homawong@gmail.com"
  },
  "files": [
    "dist",
    "lib"
  ],
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "bundle": "webpack",
    "dc": "yarn dependency-check",
    "dependency-check": "dependency-check . --unused --no-dev && dependency-check . --missing --no-dev",
    "preversion": "yarn test && yarn build && yarn dc",
    "prepublishOnly": "rimraf dist lib && yarn build && yarn bundle",
    "test": "jest",
    "watch": "jest --watch"
  },
  "dependencies": {
    "aurelia-logging": "^1.5.0",
    "color-map": "^1.1.0",
    "is-node": "^1.0.2",
    "semver": "^5.6.0"
  },
  "devDependencies": {
    "@types/semver": "^5.5.0"
  }
}
